import { ExampleComponent } from '@shiksha/common-lib'

const Button = () => {
    return (
        <> 
        <ExampleComponent text="Create React Library Example 😄" />
        <button>Hello from remote</button>
        </>
    );
};
export default Button;
